\documentclass[border = 5mm]{standalone}

\usepackage{tikz}
\usepackage{../manuel/colors}
\usepackage{../manuel/tikzstyles}

\usepackage{fontspec}
\setmainfont{Poppins}

\def\u{0.8}

\begin{document}
  \huge\bfseries
  \begin{tikzpicture}
    \begin{scope}[olympicred]
      \pic at (0, 0) {barre = {value = 1, factor = 1, unit = \u}};

      \pic at ({\u + 0.5}, 0) {barre = {value = 6, factor = 3, img, unit = \u}};

      \pic at ({7*\u + 1}, 0) {barre = {value = 11, sizes = {4, 4, 3}, img, unit = \u}};

      \pic at ({18*\u + 1.5}, 0) {barre = {value = 16, factor = 4, img, unit = \u}};

      \pic at ({34*\u + 2}, 0) {barre = {value = 21, sizes = {4, 4, 4, 4, 5}, img, unit = \u}};
    \end{scope}

    \begin{scope}[yshift = -3.5cm, olympicviolet]
      \pic at (0, 0) {barre = {value = 2, factor = 2, img, unit = \u}};

      \pic at ({2*\u + 0.5}, 0) {barre = {value = 7, sizes = {4, 3}, img, unit = \u}};

      \pic at ({9*\u + 1}, 0) {barre = {value = 12, factor = 4, img, unit = \u}};

      \pic at ({21*\u + 1.5}, 0) {barre = {value = 17, sizes = {4, 4, 4, 5}, img, unit = \u}};

      \pic at ({38*\u + 2}, 0) {barre = {value = 22, sizes = {4, 4, 4, 5, 5}, img, unit = \u}};
    \end{scope}


    \begin{scope}[yshift = -7cm, olympicgreen]
      \pic at (0, 0) {barre = {value = 3, factor = 3, img, unit = \u}};

      \pic at ({3*\u + 0.5}, 0) {barre = {value = 8, factor = 4, img, unit = \u}};

      \pic at ({11*\u + 1}, 0) {barre = {value = 13, sizes = {4, 4, 5}, img, unit = \u}};

      \pic at ({24*\u + 1.5}, 0) {barre = {value = 18, factor = 3, img, unit = \u}};

      \pic at ({42*\u + 2}, 0) {barre = {value = 23, sizes = {4, 4, 4, 4, 4, 3}, img, unit = \u}};
    \end{scope}


    \begin{scope}[yshift = -10.5cm, olympicyellow]
      \pic at (0, 0) {barre = {value = 4, factor = 4, img, unit = \u}};

      \pic at ({4*\u + 0.5}, 0) {barre = {value = 9, sizes = {4, 5}, img, unit = \u}};

      \pic at ({13*\u + 1}, 0) {barre = {value = 14, sizes = {5, 5, 4}, img, unit = \u}};

      \pic at ({27*\u + 1.5}, 0) {barre = {value = 19, sizes = {5, 5, 5, 4}, img, unit = \u}};

      \pic at ({46*\u + 2}, 0) {barre = {value = 24, factor = 4, img, unit = \u}};
    \end{scope}


    \begin{scope}[yshift = -14cm, olympicblue]
      \pic at (0, 0) {barre = {value = 5, factor = 5, img, unit = \u}};

      \pic at ({5*\u + 0.5}, 0) {barre = {value = 10, factor = 5, img, unit = \u}};

      \pic at ({15*\u + 1}, 0) {barre = {value = 15, factor = 5, img, unit = \u}};

      \pic at ({30*\u + 1.5}, 0) {barre = {value = 20, factor = 5, img, unit = \u}};

      \pic at ({50*\u + 2}, 0) {barre = {value = 25, factor = 5, img, unit = \u}};
    \end{scope}
  \end{tikzpicture}
\end{document}

